using System;

namespace Mover
{
    class P
    {
        public static readonly string config_file = Environment.CurrentDirectory + "\\Mover.config";
        public static readonly string changelog_file = Environment.CurrentDirectory + "\\Mover.changelog";
        public static readonly string beta_file = Environment.CurrentDirectory + "\\MoverBeta.config";
        public static readonly string debug_file = Environment.CurrentDirectory + "\\MoverDebug.config";
    }
}
